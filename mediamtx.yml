# MediaMTX configuration for R58 Recorder
logLevel: info
logDestinations: [stdout]

# RTSP settings
rtspAddress: :8554

# RTMP settings
rtmpAddress: :1935

# HLS settings (separate port for HLS)
hlsAddress: :8888
hlsAlwaysRemux: yes
hlsSegmentCount: 7
hlsSegmentDuration: 1s
hlsAllowOrigin: "*"

# SRT settings
srtAddress: :8890

# Paths configuration
paths:
  cam0:
    source: publisher
    sourceProtocol: udp
    sourceOnDemand: no
  cam1:
    source: publisher
    sourceProtocol: udp
    sourceOnDemand: no
  cam2:
    source: publisher
    sourceProtocol: udp
    sourceOnDemand: no
  cam3:
    source: publisher
    sourceProtocol: udp
    sourceOnDemand: no
